状态模式：
    主要用来解决对象在多状态转换时，需要对外输出不同的行为的问题。状态和行为是一一对应的，
    状态之间可以相互转换。
    当一个对象的内在状态发生改变时，允许改变其行为，这个对象看起来改变了其类。

每个状态的行为都封装到一个类中，每次调用方法时都要判断当前是什么状态，避免出现过多的if-else

---
将对象的状态以及对应的动作抽象出来，每种状态对应的操作的不同，
本体根据状态的不同，执行不同状态的操作方法。

适用于多种状态，并且不同状态具有不同的行为。